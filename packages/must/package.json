{
  "name": "@asl-tools/must",
  "description": "Asserts a condition and logs error before throwing",
  "version": "0.0.1",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "del dist && tsc",
    "lint": "eslint .",
    "test": "vitest --run",
    "clean": "del .turbo dist coverage"
  },
  "devDependencies": {
    "@toolchain/eslint-config": "workspace:*",
    "@toolchain/ts-config": "workspace:*",
    "@toolchain/vitest-config": "workspace:*",
    "@types/node": "20.3.1"
  }
}
